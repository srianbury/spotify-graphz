import { formatPath } from '../Fuctions';

const HOME = formatPath(''); // return ... + '/'
const CALLBACK = formatPath('callback');
const LOGIN = formatPath('login');
const EXAMPLE = formatPath('example');
const PLAYLIST = formatPath('playlist');
const PROFILE = formatPath('profile');

export { HOME, CALLBACK, LOGIN, EXAMPLE, PLAYLIST, PROFILE };
